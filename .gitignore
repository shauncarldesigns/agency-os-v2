# Dependencies
node_modules/

# Build output
dist/
build/

# Tooling caches
.wrangler/
.vite/

# Environment & secrets
.env
.env.local
.env.*.local
.env.production
.env.development
.env.staging
.env.test
.dev.vars
wrangler.local.toml

# Claude Code local settings (user-specific)
.claude/settings.local.json

# OS / editor
.DS_Store
.vscode/
.idea/

# Logs
*.log

# Generated build info
**/tsconfig.tsbuildinfo

# Local archives / unpacked spec drops
files.zip

# .claude/launch.json is committed on purpose: it defines the in-session dev
# servers (backend 8788, dashboard 5174) every Claude session runs for the
# operator. No secrets belong in it.
